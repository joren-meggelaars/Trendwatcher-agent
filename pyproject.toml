[project]
name = "trendwatcher-agent"
version = "0.1.0"
description = "TrendWatcher Agent"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
